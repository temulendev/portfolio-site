{
  "name": "portfoliosite",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build:assets && astro dev",
    "build:assets": "node scripts/concat-css.mjs && node scripts/copy-scripts.mjs",
    "build": "npm run build:assets && astro build && node scripts/flatten-post-html.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.7.0"
  }
}
